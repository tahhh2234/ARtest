# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: 8.8.8.8
  become: yes

  tasks:

- name: Call Roles
  hosts: 8.8.8.8
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
