# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: google.com
  become: yes

  tasks:

- name: Call Roles
  hosts: google.com
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
